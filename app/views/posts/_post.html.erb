<div class="callout">
  <h4><%= link_to post.title, user_post_path(post.author_id, post) %></h4>
  <p><%= post.text %></p>
  <ul class="menu simple align-right">
      <li>
        <a href="#">Comments: <%= post.comments_counter || 0 %></a>
      </li>
      <li>Likes: <%= post.likes_counter || 0 %></li>
  </ul>
</div>